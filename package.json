{
  "devDependencies": {
    "tailwindcss": "^3.0.22"
  },
  "scripts": {
    "build": "NODE_ENV=production npx tailwindcss -i base.css -o tailwind.css --minify",
    "watch": "npx tailwindcss -i base.css -o tailwind.css --watch"
  }
}
